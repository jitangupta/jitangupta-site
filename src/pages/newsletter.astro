---
import Layout from '../layouts/Layout.astro';
const title = "🚀 Tech Strategy & Cloud Architecture Insights | Join Jitan Gupta's Newsletter";
const description = "Stay ahead in software architecture, cloud strategy, and consulting with exclusive insights from Jitan Gupta. Get high-impact tech strategies, case studies, and career growth blueprints—straight to your inbox. Subscribe now!";
---
<Layout	title={title} description={description}>
    <section class="bg-gray-100 mx-2">
        <div class="bg-white max-w-screen-xl mx-auto px-5 py-5 md:px-15 md:py-10 rounded-b-2xl">
            <!-- Title -->
            <h1 class="text-3xl md:text-5xl lg:text-6xl mt-5 md:mt-8 mb-2 font-bold" style="line-height: 1.2;">Stay Ahead in Tech & Business 🚀</h1>

            <!-- Convincing Notes -->
            <p>Want to future-proof your career in software architecture, cloud strategy, and high-impact consulting? Join my <b>exclusive newsletter</b>, where I share:</p>
            <ul class="list-none my-4">
                <li>💡 <b>Strategic insights</b> on scaling software, cloud migrations & enterprise architecture.</li>
                <li>🛠️ <b>Real-world</b> case studies from consulting & tech leadership.</li>
                <li>🚀 <b>Blueprints</b> for career growth in tech, AI, and SaaS consulting.</li>
                <li>🔥 <b>Early access</b> to frameworks, tools & premium resources.</li>
            </ul>
            <p>No fluff. No spam. Only <b>high-value</b> insights to help you <b>architect smarter</b>, <b>scale faster</b>, and <b>lead with confidence</b>.</p>
            <p>📩 Subscribe now and stay ahead of the curve!</p>

             <!-- Subscription Form -->
             <form id="newsletter-form" class="mt-6 flex flex-col sm:flex-row gap-4">
                <input 
                    type="email" 
                    id="email-input" 
                    placeholder="Enter your email" 
                    class="w-full sm:w-2/3 px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" 
                    required 
                />
                <button 
                    type="submit" 
                    class="w-full sm:w-auto bg-secondary-600 text-white font-semibold px-6 py-3 rounded-lg transition-all duration-300">
                    Subscribe
                </button>
            </form>

            <!-- Message Display -->
            <p id="message" class="mt-4 text-sm"></p>
        </div>
    </section>

    <!-- Client-side JavaScript -->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const form = document.getElementById("newsletter-form");
            const emailInput = document.getElementById("email-input");
            const message = document.getElementById("message");

            form?.addEventListener("submit", async function (event) {
                event.preventDefault();
                message.textContent = ""; // Clear previous message
                message.className = "mt-4 text-sm"; // Reset classes

                const email = emailInput.value.trim();
                if (!email) {
                    message.textContent = "❌ Please enter a valid email.";
                    message.classList.add("text-red-600");
                    return;
                }

                const res = await fetch("https://api.jitangupta.com/api/subscribe", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ email }),
                });

                if (res.ok) {
                    message.textContent = "✅ Subscription successful!";
                    message.classList.add("text-green-600");
                    emailInput.value = "";
                } else {
                    message.textContent = "❌ Subscription failed. Try again.";
                    message.classList.add("text-red-600");
                }
            });
        });
    </script>
</Layout>