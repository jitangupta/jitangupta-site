---
import PaperLayout from "../layouts/PaperLayout.astro";
import { builds } from "../data/builds";

const title = "Builds — Jitan Gupta";
const description = "Open source tools and projects, built in production, documented in public.";

const categoryLabel: Record<string, string> = {
  "production":          "Production",
  "personal-production": "Personal production",
  "supporting-system":   "Supporting system",
  "workflow-template":   "Workflow template",
};

const categoryOrder = ["production", "personal-production", "supporting-system", "workflow-template"] as const;

const buildDates: Record<string, string> = {
  "cb-mcp-server": "2026",
  "PromptMate": "2025 → ongoing",
  "Cowork Boilerplate": "2026",
  "File Safety Guard": "2026",
  "Content Board": "2026",
  "jitangupta.com": "2024 → ongoing",
};

const grouped = categoryOrder
  .map(cat => ({ cat, items: builds.filter(b => b.category === cat) }))
  .filter(g => g.items.length > 0);
---

<PaperLayout title={title} description={description}>

  <style>
    .page-hero h1 { margin-bottom: 16px; }
    .page-hero .meta-row {
      display: flex; gap: 24px; flex-wrap: wrap;
      margin-top: 28px;
      padding-top: 24px;
      border-top: 1px solid var(--hairline);
      font-family: "Geist Mono", monospace;
      font-size: 12px;
      color: var(--muted);
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }
    .page-hero .meta-row b { color: var(--ink); font-weight: 500; }

    .build-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1px;
      background: var(--hairline);
      border: 1px solid var(--hairline);
    }
    .build-grid .build-card:last-child:nth-child(odd) { grid-column: 1 / -1; }
    .build-card {
      background: var(--bg);
      padding: 36px;
      display: flex;
      flex-direction: column;
      gap: 16px;
      transition: background 200ms ease;
      position: relative;
    }
    .build-card:hover { background: var(--bg-tint); }
    .build-card .top { display: flex; justify-content: space-between; align-items: center; gap: 16px; }
    .build-card h3 { font-size: clamp(22px, 2.2vw, 28px); font-weight: 500; letter-spacing: -0.02em; line-height: 1.1; }
    .build-card .body { font-size: 15.5px; line-height: 1.6; max-width: 60ch; }
    .build-card .tag-row { display: flex; flex-wrap: wrap; gap: 6px; margin-top: auto; padding-top: 12px; }
    .build-card .footer-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 16px;
      padding-top: 16px;
      border-top: 1px solid var(--hairline);
      font-family: "Geist Mono", monospace;
      font-size: 12px;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      color: var(--muted);
    }
    .build-card .footer-row .open { color: var(--ink); display: inline-flex; align-items: center; gap: 6px; }
    .build-card:hover .footer-row .open { color: var(--accent); }
    @media (max-width: 820px) { .build-grid { grid-template-columns: 1fr; } }

    .category-group { margin-bottom: 48px; }
    .category-group:last-of-type { margin-bottom: 0; }
    .category-header {
      padding: 16px 0 12px;
      border-bottom: 1px solid var(--hairline);
      margin-bottom: 1px;
    }
  </style>

  <section class="page-hero">
    <div class="wrap reveal">
      <span class="eyebrow">Builds</span>
      <h1 class="display" style="font-size: clamp(40px, 5.4vw, 76px);">
        Things I built because I needed them.
      </h1>
      <p class="lede" style="margin-top: 24px;">
        Open source, in production, documented end-to-end. Every project on this page started as a real problem inside my own work — then got shaped into something other engineers can pick up and run.
      </p>
      <div class="meta-row">
        <span><b>1</b> Production</span>
        <span><b>{builds.filter(b => b.category === "personal-production").length}</b> Personal production</span>
        <span><b>{builds.filter(b => b.category === "supporting-system" || b.category === "workflow-template").length}</b> Templates & tooling</span>
        <span><b>All</b> Open source</span>
      </div>
    </div>
  </section>

  <section class="tight">
    <div class="wrap">
      {grouped.map(({ cat, items }) => (
        <div class="category-group reveal">
          <div class="category-header">
            <span class="eyebrow">{categoryLabel[cat]}</span>
          </div>
          <div class="build-grid">
            {items.map((build) => {
              const href = build.githubUrl || build.liveUrl || "https://github.com/jitangupta";
              const isExternal = href.startsWith("http");
              const linkLabel = build.githubUrl ? "View on GitHub →" :
                                build.liveUrl   ? "View live →" :
                                                  "Browse GitHub →";
              const dateStr = buildDates[build.title] || "2025";
              return (
                <a class="build-card reveal"
                   href={href}
                   target={isExternal ? "_blank" : undefined}
                   rel={isExternal ? "noopener noreferrer" : undefined}>
                  <div class="top">
                    <h3>{build.title}</h3>
                    <span class={`status${build.status !== "live" ? " muted" : ""}`}>
                      {categoryLabel[build.category]}
                    </span>
                  </div>
                  <p class="body">{build.description}</p>
                  <div class="tag-row">
                    {build.techStack.slice(0, 4).map((tech) => (
                      <span class="tag">{tech}</span>
                    ))}
                  </div>
                  <div class="footer-row">
                    <span>{dateStr}</span>
                    <span class="open">{linkLabel}</span>
                  </div>
                </a>
              );
            })}
          </div>
        </div>
      ))}

      <div class="build-grid" style="margin-top: 1px;">
        <a class="build-card reveal" href="https://github.com/jitangupta" target="_blank" rel="noopener noreferrer">
          <div class="top">
            <h3>More on GitHub</h3>
            <span class="status muted">Experiments</span>
          </div>
          <p class="body">
            Smaller experiments, demos, and one-off scripts that haven't graduated into a full project yet — or might become the next one.
          </p>
          <div class="tag-row">
            <span class="tag">Experiments</span>
            <span class="tag">Scripts</span>
            <span class="tag">Demos</span>
          </div>
          <div class="footer-row">
            <span>Various</span>
            <span class="open">Browse GitHub →</span>
          </div>
        </a>
      </div>
    </div>
  </section>

  <section class="tight">
    <div class="wrap">
      <div style="display: grid; grid-template-columns: 1fr auto; gap: 40px; align-items: end; padding: 56px 0; border-top: 1px solid var(--hairline); border-bottom: 1px solid var(--hairline);">
        <div>
          <span class="eyebrow">Patterns</span>
          <h2 class="h2" style="margin-top: 14px;">When something repeats across builds, it becomes a framework.</h2>
        </div>
        <div style="display: flex; gap: 12px; flex-wrap: wrap;">
          <a class="btn primary" href="/frameworks">
            See the frameworks
            <svg class="arrow" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 7h8M7.5 3.5L11 7l-3.5 3.5"/></svg>
          </a>
        </div>
      </div>
    </div>
  </section>


</PaperLayout>
