# ChatGPT Apps SDK Wrapper

This folder will contain the ChatGPT-native wrapper around the MCP server.

Responsibilities:

- app metadata
- tool descriptions and annotations
- optional ChatGPT UI components
- public assets and screenshots
- release-specific configuration

Keep the first version simple. Add UI only after the MCP tools are useful in real conversations.
